import { LogRequest2Interceptor } from './log-request-2.interceptor';

describe('LogRequest2Interceptor', () => {
  it('should be defined', () => {
    expect(new LogRequest2Interceptor()).toBeDefined();
  });
});
